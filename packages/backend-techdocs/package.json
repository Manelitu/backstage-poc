{
  "name": "backend-techdocs",
  "version": "0.0.0",
  "main": "dist/index.cjs.js",
  "types": "src/index.ts",
  "private": true,
  "backstage": {
    "role": "backend"
  },
  "scripts": {
    "start": "backstage-cli package start --config ../../app-config.yaml --config ../../app-config.techdocs.yaml",
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean",
    "build-image": "docker build ../.. -f packages/backend-techdocs/Dockerfile --tag backstage-techdocs"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^0.17.0",
    "@backstage/backend-plugin-api": "^1.9.0",
    "@backstage/config": "^1.3.7",
    "@backstage/plugin-techdocs-backend": "^2.1.7",
    "backend-common": "workspace:*",
    "better-sqlite3": "^12.0.0",
    "node-gyp": "^10.0.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.1"
  },
  "files": [
    "dist"
  ]
}
